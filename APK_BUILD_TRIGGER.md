# Operator APK Build

The debug APK is produced by GitHub Actions from the `feature/operator-command-contract-v1` branch.

Workflow: **Build Operator APK**

Artifact: **operator-apk-debug**

This APK is for UI testing only. It does not connect to the municipal backend or send real valve commands.
