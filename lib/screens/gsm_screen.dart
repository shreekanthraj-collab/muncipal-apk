import 'package:flutter/material.dart';

import 'valve_view_screen.dart';

class GsmScreen extends StatelessWidget {
  const GsmScreen({super.key});

  @override
  Widget build(BuildContext context) => const ValveViewScreen(isLora: false);
}
